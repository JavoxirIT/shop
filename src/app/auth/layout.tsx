export const metadata = {
    title: 'SALE | AUTH',
    description: '',
    keywords: '',
};

export default function AuthLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
